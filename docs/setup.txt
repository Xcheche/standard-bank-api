#for creating static folders 
mkdir -p static && touch static/{css,js,img}/.gitkeep


#for creating requirements.txt
mkdir -p requirements && touch requirements/{base,local,production}.txt

#for creating multiple django settings
mkdir -p settings && touch settings/{__init__,base,local,production}.py


#Generate secret key
python -c "import secrets; print(secrets.token_urlsafe(100))"

#make .env files folder and example env file
mkdir -p .envs && touch .envs/.env.local && touch .envs/.env.example

#Upgrade pip and setuptools
pip install --upgrade pip setuptools 
#create project apps
mkdir -p core_apps && touch core_apps/__init__.py

./manage.py startapp user_auth 
./manage.py startapp common
./manage.py startapp user_profile
